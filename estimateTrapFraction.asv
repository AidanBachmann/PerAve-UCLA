function [ft] = estimateTrapFraction(param,thetap)
    ub = 1.5*pi; % Phase upper bound
    lb = -1.5*pi; % Phase lower bound
    ft = zeros(param.Nsnap,1); % Trap fraction along undulator
    for ii 1:param.Nsnap
        counter = 0;
        for jj 1:param.Np
            if thetap(ii,1,)

end